buildscript {
    ext {
        swaggerVersion = "2.6.1"
        springBootVersion = '1.5.6.RELEASE'
        jacksonVersion = '2.8.9'
        rxJavaVersion = "2.1.2"
        kotlinVersion = '1.1.51'
    }
    repositories {
        jcenter()
    }
}

allprojects {
    repositories {
        jcenter()
    }
}